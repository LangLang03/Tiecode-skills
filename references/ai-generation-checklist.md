# AI 生成检查单（硬约束）

## 生成前
- [ ] 【顶级硬规则-阻断】所有读取/写入均使用 UTF-8 无 BOM（未使用系统默认代码页）；若出现乱码已先按 UTF-8 重读并校正
- [ ] 【硬性需求-阻断】若类带 `@禁止创建对象`，未出现任何实例化（含 `变量 名称 : 类型` 自动创建与 `变量 名称 : 类型 = 创建 类型()` 显式创建）；若命中违规，已先停止生成并修复
- [ ] 【最高优先级】若支持 Agent 且用户未明确禁止，已使用 Agent 完成文档查看与项目理解
- [ ] 【硬性需求-阻断】若支持 Agent 且用户未禁止，已明确本轮也会执行代码审查（不得复用首轮/上一轮审查结果）
- [ ] 【最高优先级】已确认目标运行时为 Android（非 Java SE）
- [ ] 【最高优先级】若涉及 `@导入Java`/`@code`，所有 Java API 已完成 Android 可用性校验（Android框架/依赖库/项目证据）
- [ ] 【硬性需求-阻断】用户未明确指定 AndroidX 时，未生成任何 AndroidX 导入/依赖/API（`androidx.*`）
- [ ] 已识别当前为“项目 skill”还是“全局 skill”
- [ ] 文档根目录已锁定为当前已加载 skill 的 `references/`，未跨工作区乱搜 `skills/**` 文档
- [ ] Python 可用时，已运行 `scrpits/list_doc_absolute_paths.py` 并优先使用输出的绝对路径读取文档
- [ ] Python 不可用时，已回退使用 `SKILL.md` 中原有 Mandatory Read Set 相对路径
- [ ] 默认不写 `包名`（除非用户明确要求）
- [ ] 已尽量读取更多相关文档以提高准确率
- [ ] 若支持 Agent 且用户未禁止，Agent 检索过程已去重
- [ ] 已完成“必读文件列表”而非只读少量文件（2-3 个文件不允许直接开写）
- [ ] 开始编码前已输出“已读取文件清单”
- [ ] 新增与修改代码仅写在 `源代码/`，不写入 `绳包/` 任何位置
- [ ] 已执行第0标识：完成继承链分析（当前类/父类/对象可上转类型），并确认该步骤覆盖所有类（不止窗口类）
- [ ] 已确认属性访问语义：`属性读` 对应 `对象.属性` 读取，`属性写` 对应 `对象.属性 = 值` 赋值
- [ ] 已确认布局字段（宽度/高度/位于某组件之下等）有可检索的属性读写支持或 `@布局属性` 方法支持；内置结构键 `父布局/根布局` 允许直接使用
- [ ] `@布局属性` 方法签名已校验为双参数（`欲设置组件 : 可视化组件` + 参数值）
- [ ] `@布局配置` 中布局属性方法字段已使用 `@方法名=值` 形式（如 `@位于某组件之下=1`）
- [ ] 已确认 `@位于某组件之下/上` 仅表示 2D 平面位置关系，不被误用于改变父子层级或 z 顺序
- [ ] 布局值类型已匹配目标签名：文本值加引号、逻辑型用 `真/假`、数字值不加引号（如 `内容="提示"`,`字体大小=18`,`@位于布局中间=真`）
- [ ] 布局颜色字段（如 `背景颜色`）已使用十进制有符号整数，未使用 `0x/0X` 十六进制字面量
- [ ] 对象变量已使用自动创建语义：`变量 名称 : 类型`；未生成 `变量 名称 : 类型 = 创建 类型()`
- [ ] 若类型带 `@禁止创建对象`，已先查看类定义并确认可用静态成员；未生成任何实例化（含 `变量 名称 : 类型` 自动创建与 `= 创建 类型()`）
- [ ] 需要只声明不创建时，已使用 `变量 名称 : 类型?`（并确认未触发自动创建）
- [ ] 标识符（类/方法/变量/参数/常量/事件）未使用语法关键字命名
- [ ] 数组类型声明使用后缀 `[]`，并按需要支持多维（`类型[]`、`类型[][]`、`类型[][][]`）
- [ ] 若使用数组字面量初始化，已采用花括号语法（如 `变量 名称 : 整数[] = {1,2,3}`），并确认元素类型与维度匹配
- [ ] `小数 -> 单精小数` 未使用直接转换；仅在必须转换时使用 `到文本().到单精小数()`（或项目中已定义等价方法）或 `@code`
- [ ] 多分支条件已使用 `否则 条件` 写法，未生成 `否则如果`
- [ ] 多分支守卫分支未生成 `否则 条件 则`（`否则 条件` 后无 `则`）
- [ ] 条件循环已使用 `循环(条件)`，未生成 `循环 条件`
- [ ] 需要提前退出循环时，结绳层已使用 `退出循环` 关键字，未生成结绳层 `break`
- [ ] 函数调用参数已匹配定义签名（数量/类型），未生成不存在的重载形式（如未验证的 `到小数(文本)`）
- [ ] 函数调用形态已匹配方法类别：实例方法使用 `对象.方法(...)`，静态方法使用 `类型.方法(...)`；未生成 `到文本(变量名)` 这类全局误调用
- [ ] 已按“文件可多顶层类”语法组织代码，且未生成内部类/嵌套类
- [ ] `@导入Java` 已按类独立声明，未把前一类导入作用域错误复用到后一类
- [ ] 若使用 `@外部Java文件` + `@导入Java("包名.类型名")` 封装组件，`@code` 中 `onCreateView/getView` 返回类型、局部变量与强转均使用简单类型名（`类型名`），未误写包前缀类型名
- [ ] 权限/清单注解语法已核对（`@附加权限`、`@附加清单*`、`@附加可变清单`、`@编译时处理参数`）
- [ ] `@附加可变清单` 已采用“单独类 + 单个空方法”参数映射模式（如 `方法 XXX(输入法类名:文本,输入法名称:文本,输入法配置:文本)`）
- [ ] 已输出关键注解解释（作用目标/用途/关键参数），并说明无效注解会在编译期清理
- [ ] 布局键名全部使用中文规范键（`高度/宽度`），未出现 `height/width` 等英文键
- [ ] UI 文本字面量无无意空格（如 `猜正面` 而非 `猜 正面`；分隔符默认不自动加空格）
- [ ] 主窗口类名与继承正确：`类 启动窗口 : 窗口`
- [ ] 页面继承 `窗口`，不是 `组件容器`
- [ ] 未滥用 `@全局类` / `@全局基础类`（仅在明确全局入口需求时使用）
- [ ] 若当前类继承 `窗口`（含间接继承），已把 `本对象` 直接当作 `安卓环境(Context)` 传参
- [ ] 未在窗口继承类中生成 `本对象.取安卓环境()`
- [ ] 调用函数前已在 `源代码/` 与 `绳包/` 检索其存在
- [ ] 不假设任何“未公开内置函数”
- [ ] 已选中最接近的模板（类/属性/事件/组件/工具类）
- [ ] 仅当要写内嵌 Java 时才准备 `@导入Java`
- [ ] 未引入 Java SE 专属且 Android 不可用的 API（如 `java.awt.*`、`javax.swing.*`、`java.applet.*`）
- [ ] 未在“用户未指定 AndroidX”场景下引入 AndroidX 依赖或 API
- [ ] 若为映射封装，已准备 `@指代类`

## 生成中
- [ ] 若检测到 `@禁止创建对象` 类被实例化，已立即中止继续生成并先修复该违规
- [ ] 所有块都有闭合 `结束 xxx`
- [ ] `结束 类` 与类声明逐一对应，未跨层闭合且未夹带内部类声明
- [ ] 方法参数均有类型，默认值在尾部
- [ ] 返回类型写法在文件内一致（`:` 或 `为`）
- [ ] 仅必要时使用 `@code`，且 `@code/@end` 成对
- [ ] Java 嵌入中 结绳 变量全部使用 `#`
- [ ] 运算符方法配 `@运算符重载`
- [ ] 已检查构造器样板是否已定义；仅在缺失且必要时补一份
- [ ] 布局采用 `@布局配置` 父子链路声明（父先子后）
- [ ] 使用布局属性方法字段时，已验证其带 `@布局属性` 注解且签名可匹配
- [ ] 多组件布局声明块格式正确：类定义后空一行；`@布局配置`+`变量` 对之间无空行，且相邻 `@布局配置` 定义之间无空行（布局块结束后的空行不作约束）
- [ ] 同名 `属性读/属性写` 成对关系正确，避免只定义其一却双向使用
- [ ] 条件判断使用正确逻辑符号：比较相等/不等用 `==/!=`，未把 `=` 当比较符（例如未写 `如果 硬币结果 = 0 则`）
- [ ] `退出循环` 仅出现在循环体内，未出现循环外使用

## 生成后
- [ ] 输出与改写文件编码已复核为 UTF-8 无 BOM
- [ ] 已复查“硬性需求-阻断”项：无 `@禁止创建对象` 类实例化残留
- [ ] 【硬性需求-阻断】本轮已完成代码审查并处理问题；未以“首轮已审查”或“上一轮已审查”替代
- [ ] 事件声明与事件实现未混用
- [ ] `容错处理()/结束容错()` 配对
- [ ] 线程块 `提交到新线程运行()/结束提交到新线程()` 配对
- [ ] 命名符合动宾/语义化规范
- [ ] 无“发明语法”与未知注解
- [ ] 代码审查已覆盖函数调用形态（存在性 + 签名 + 静态/实例）并修复问题
- [ ] 代码审查已覆盖 `@禁止创建对象` 类的调用约束（仅静态调用、无实例化、必要时使用 `类型?` 声明）
- [ ] 代码审查已覆盖标识符关键字冲突与数组维度声明合法性
- [ ] 代码审查已覆盖数组花括号初始化语法与元素类型一致性
- [ ] 代码审查已覆盖 `小数 -> 单精小数` 约束（无直接转换、仅必要转换、路径为 `到文本().到单精小数()` 或 `@code`）
- [ ] 代码审查已覆盖 `退出循环` 关键字使用（循环内合法、未误用 `break`、未在循环外使用）
- [ ] 代码审查已覆盖 `@位于某组件之下/上` 语义（仅平面排布，不改变层级/z 顺序）
- [ ] 代码审查已覆盖 AndroidX 约束（未指定不引入，指定后再校验可用性）
- [ ] 代码审查已覆盖组件封装类型规范（外部 Java 组件包装时，`onCreateView/getView` 使用导入后的简单类型名，不混用包前缀类型名）
- [ ] 代码审查已覆盖注解语义（作用域/作用目标/编译期消费），并标注潜在被编译清理的无效注解
- [ ] 【最高优先级】若支持 Agent 且用户未明确禁止，已使用 Agent 审查本轮生成代码并修复发现问题

## 回归
- [ ] 与同目录现有 `.t` 风格一致
- [ ] 新增注解/关键字可在仓库中找到先例
- [ ] 若新增复杂逻辑，补最小注释说明意图


- [ ] `@布局配置` 的数字布局值支持 `px/dp`；未显式单位时按 `px` 处理；除非有明确需求，优先使用 `dp`。

- [ ] 涉及布局底层实现时，已按 Java 路径核验：要么存在可检索的对应实现类，要么已通过 `@code` 完成可追踪封装。

- [ ] 不假设使用者操作一定正确：已对输入、状态、边界和错误路径进行严格且全面检查，确认不存在可触发崩溃的明显缺口。
